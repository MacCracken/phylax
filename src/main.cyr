# phylax — Threat detection engine entry point
# Includes the library and provides the binary entry point

include "src/phylax.cyr"

# ================================================================
# Entry point — invoke main and exit with its return code
# ================================================================

var r = main();
syscall(SYS_EXIT, r);
